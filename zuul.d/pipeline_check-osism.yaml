---
- pipeline:
    name: check-osism
    description: |
      Checks the status of OSISM Testbed and merges with local copy if needed
    manager: independent
    trigger:
      github.com/osism/testbed:
        - event: pull_request
          action:
            - opened
            - synchronize
      